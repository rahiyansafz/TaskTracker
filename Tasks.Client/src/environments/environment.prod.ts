export const environment = {
  production: true,
  apiUrl: 'https://localhost:7009/api', // when you release your app to production, this should change to whatever host or address your API will be deployed to
};
